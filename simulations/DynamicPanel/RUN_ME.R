# This script runs the simultaneous model and moment selection simulation
# exercise from Section 6.2 of the paper. Before running this script, create
# a subdirectory of the directory in which this script is stored called 'results'

source('calculate_RMSE.R')

source('make_figures.R')

source('make_tables.R')